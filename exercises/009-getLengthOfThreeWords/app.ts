function getLengthOfThreeWords(word1: string, word2: string, word3: string): number {
  // your code here
  return word1.length + word2.length + word3.length;
}

let output = getLengthOfThreeWords('some', 'words', 'here');
console.log(output);

export {};
